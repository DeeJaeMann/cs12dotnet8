/* Visual Studo 2022: run app, change message, click Hot Reload
 * Visual Studio Code: run app using dotnet watch, change message
 */

while (true)
{
    WriteLine("Goodbye, Hot Reload!");
    await Task.Delay(2000);
}